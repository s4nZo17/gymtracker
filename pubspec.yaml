name: gymtracker
description: Il tuo diario di allenamento personale
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.2
  csv: ^6.0.0
  fl_chart: ^0.68.0
  intl: ^0.19.0
  share_plus: ^9.0.0
  google_fonts: ^6.2.1
  flutter_slidable: ^3.1.0
  file_picker: ^8.0.0+1
  provider: ^6.1.2
  flutter_colorpicker: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
